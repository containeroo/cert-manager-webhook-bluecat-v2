# Solver testdata directory

`config.json` is loaded by conformance tests as the `webhook.config` payload.

The example config here is shaped for the BlueCat Address Manager REST v2
solver in `main.go`.
